# Babel extraction config for SigViewer.
# Only Python sources contain translatable strings (menu labels are re-set in
# code via _(), verdicts are wrapped in _()). The pure crypto core and
# stdout/stderr debug text are intentionally NOT extracted.
[python: signature_viewer_core.py]
[python: i18n.py]
